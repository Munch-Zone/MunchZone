<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical"
    >

 <include
     
     layout="@layout/header_menu" />

  <include layout="@layout/footer" />
  <!-- 
    <ScrollView
    android:id="@+id/scrollableContents"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_below="@id/header_menu"
    android:background="#b7c723"
     >


  </ScrollView>
    -->  
    <ExpandableListView
                android:id="@+id/menuLvExp"
                android:childDivider="@drawable/child_separator"
                android:background="#ffffff"
                android:dividerHeight="1dp"
                android:divider="#1b8d11"
                android:layout_height="wrap_content"
                android:layout_width="match_parent"
               android:choiceMode = "singleChoice"
            
      android:layout_below="@id/header_menu"
                android:groupIndicator="@android:color/transparent"
               
               />
    

               
<ListView 
     android:id="@+id/Menulistview"
     android:layout_width="match_parent" 
     android:layout_height="0dip" 
     android:visibility="gone"
   
      android:background="#ffffff"
      android:dividerHeight="1dp"
      android:layout_above="@id/footer"
   
    android:layout_below="@id/header_menu"
      android:divider="#1B8D11" />

<ScrollView
    android:id="@+id/scrollableContents"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
   
    android:layout_below="@id/header_menu"
    android:visibility="gone" >

  </ScrollView>
</RelativeLayout>


<!-- 
android:scrollbars="vertical"
   android:layout_toLeftOf="@+id/menuLvExp"
   android:layout_toStartOf="@+id/menuLvExp"
    -->